---
apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: staging
  labels:
    app: inference-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: inference-service
